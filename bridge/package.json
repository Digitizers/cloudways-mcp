{
  "name": "cloudways-mcp-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Pinned dependency graph for the mcp-remote bridge used by Claude Desktop. Install with `npm ci` so the committed package-lock.json decides every version.",
  "license": "MIT",
  "dependencies": {
    "mcp-remote": "0.14.0"
  }
}
